<%- include /comm/header %>
<%- include /comm/nav %>

<div class="container" >
<p><p><p>
欢迎<br>
展示note的namespace和category，并进行维护<br>
并搜索note笔记

<div class="container" >
  <div  >
    <div  >
      <div  >
        <div >
          <table id="listResult" class="table table-hover">
            <thead>
              <tr>
              	<th>序号</th>
                <th>namespace</th>
                <th>category</th>
              </tr>
            </thead>
            <tbody>
              <% result.categorys.forEach(function (category, index) { %>
              <tr >
              	<td><%= index+1 %></td>
                <td><%= category.namespace %></td>
                <td><%= category.category %></td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="row" style="display:none;">
  <div class="panel-body">
    <button type="button" id="popAddCategory" class="btn btn-outline btn-primary">添加分类</button>
  </div>
</div>
<div id="addCategoryBlock" style="display:none;">
  <form id="addRuleForm" method="post">
    <%/*
    <div class="form-heading">
      <h2>笔记分类</h2>
    </div>
    */%>
    <input type="text" placeholder="namespace" name="namespace" required="true" class="form-control"/>
    <input type="text" placeholder="category" name="category" required="true" class="form-control"/>
 	<!-- <button type="button" id="addRule" class="btn btn-lg btn-primary btn-block">添加</button> -->
    <div id="result">
      <%= (result.result || "") %>
    </div>
  </form>
</div>

</div>
<%- include /comm/footer %>